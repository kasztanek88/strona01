{% extends 'user_account/layout.html.twig' %}


    {% block two %}

        <div class="container" style="padding:50px;">
            <div class="row uniform 50%">
                <div class="6u 12u$(4)"">
                <h1>New Article</h1>
                {{ form_start(newart) }}
                {{ form_row(newart.title) }}
                {{ form_row(newart.artContent | raw, {'label' : 'Enter the text'}) }}
                <button type="submit" class="button" formnovalidate>Save</button>
                {{ form_end(newart) }}
            </div>
        </div>
        </div>
    {% endblock %}

